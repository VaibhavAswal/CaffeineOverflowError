import styles from "./profile.module.css";

const Profile = () => {
  return (
    <div className={styles.profile}>
      <p>Hello, sign in</p>
      <p>Account & details</p>
    </div>
  );
};

export default Profile;
